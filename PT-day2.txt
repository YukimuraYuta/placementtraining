(int/void) function_name(){}--> c/c++

public/private/protected/default/static  return type method_name(){}--> java

static stops going it to 


heaps -- usage: max_heap :the max value is with root node so the time complexity is O(n)


Q) n= 123 return 321:

s=n/10
rev=rev*10+3
n=n/10


%x -- to get hexadecimal address
%p -- to get the complete address

static -- makes the pointer global and solve dangling problem ---


Storage Classes :

									Scope							Lifetime



*static									global							file

*extern									global							multiple files inside same folder

*auto									local							{    }

*register								RAM							RAM



BAKER'S PARTITION PROBLEM  and max regions using straight lines are the same:

\


RSB= rightmost set bit=xorall & -xorAll






paackage:

It is a collection of similar classes and interfaces

It is  actually a folder in our project or application

In terms of IT industery, we call it package

package === folder


Industry Std for package :{TCS|ICICI|login|login.java}

com.tcs.icici.login.login.java;


jagged ARRAYS: 
2D arrays with unequal colums in called a jacked array


for (int []numbers: nums){
for (int n :numbers){
print(n);
}
}

new - keyword in C++ is reserved for  Dynamic Memory allocation adn not for object creation
































































































